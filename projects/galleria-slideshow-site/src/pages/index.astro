---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const gallery = await getCollection('gallery');
---

<Layout>
  <h1>Galleria</h1>
  <p>Found {gallery.length} paintings in the gallery.</p>
</Layout>
